meta {
  name: Meetups: Request to join
  type: http
  seq: 1
}

post {
  url: {{endpoint}}/meetups/{{meetupId}}/requests
  body: json
  auth: inherit
}

headers {
  x-user-id: {{userId}}
}

body:json {
  {
    "message": "Requested to join!"
  }
}

settings {
  encodeUrl: true
}
