meta {
  name: Meetups: Request to join
  type: http
  seq: 1
}

post {
  url: {{endpoint}}/meetups/{{meetupId}}/requests
  body: json
  auth: inherit
}

body:json {
  message: "Hey, would love to join!"
}

headers {
  x-user-id: {{userId}}
}

settings {
  encodeUrl: true
}

