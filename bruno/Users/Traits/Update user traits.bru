meta {
  name: Update user traits
  type: http
  seq: 2
}

post {
  url: {{base_url}}/users/traits
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "traitIds":[10,11]
  }
}

settings {
  encodeUrl: true
}
