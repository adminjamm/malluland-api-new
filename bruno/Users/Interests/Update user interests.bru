meta {
  name: Update user interests
  type: http
  seq: 2
}

post {
  url: {{base_url}}/users/interests
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "interestIds":[22,12]
  }
}

settings {
  encodeUrl: true
}
