meta {
  name: List favorites
  type: http
  seq: 6
}

get {
  url: {{base_url}}/users/user-favorites?category=movies
  body: none
  auth: bearer
}

params:query {
  category: movies
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
