meta {
  name: Update profile
  type: http
  seq: 3
}

put {
  url: {{base_url}}/users/profile
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name":"hdcvdhcvd"
  }
}

settings {
  encodeUrl: true
}
