meta {
  name: Requests: Chat archive
  type: http
  seq: 1
}

post {
  url: {{endpoint}}/requests/chats/{{requestId}}/archive
  body: none
  auth: inherit
}

headers {
  x-user-id: {{userId}}
}

settings {
  encodeUrl: true
}
